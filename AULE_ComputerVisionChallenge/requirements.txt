# Runtime dependencies of the notebooks and the aulecv package.
# Google Colab already provides all of them; the first cell of each notebook
# installs any that are missing there.
numpy>=1.23
opencv-python>=4.6        # opencv-python-headless also works; no GUI calls are made
matplotlib>=3.7        # 3.6 breaks %matplotlib inline with current matplotlib-inline
pandas>=1.5
imageio>=2.25             # GIF export in Part D
pillow>=9.0               # imageio's GIF backend
